<%= form_for(@user, :url => { :action => "update_profile_image" } ) do |f| %>

 <div class="form-group">
 <%= f.file_field :img, required: true %>
 </div>
 <div class="form-group">
 <%= f.submit "Update", class: "btn btn-danger" %>
</div>
 <% end %>

